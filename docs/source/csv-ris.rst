CSV Resource Identifier Service
===============================

This service identifies subjects, sessions, and scans in an image archive by reading a
CSV file and applying the metadata from each row to set the properties on the subject,
session(s), and each scan.

